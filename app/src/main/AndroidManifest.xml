<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
package="com.example.newsapp">

    <uses-permission android:name="android.permission.INTERNET"/>

<application
    android:allowBackup="true"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:supportsRtl="true"
    android:theme="@style/Theme.NewsApp">
    <activity android:name=".Category.Technology" />
    <activity android:name=".Category.Entertainment" />
    <activity android:name=".Category.Science" />
    <activity android:name=".Category.Health" />
    <activity android:name=".Category.Sports" />
    <activity android:name=".App" />
    <activity android:name=".Feed" />
    <activity android:name=".Detailed" />
    <activity android:name=".MainActivity">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
    </activity>
</application>

</manifest>